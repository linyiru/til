checkout:
  post:
    - git submodule sync
    - git submodule update --init

deployment:
  production:
    branch: master
    commands:
      - git config --global user.name "Circle CI"
      - git config --global user.email "deduce@gmail.com"
      - hexo clean
      - hexo generate
      - hexo deploy
      - bin/automated

test:
  override:
    - echo "Passed"
